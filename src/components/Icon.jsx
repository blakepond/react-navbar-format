import logo from "../trifecta.svg";
import React from "react";

const Icon = () => {
   return (
      <header className="App-header">
         <img
            src={logo}
            className="App-logo"
            alt="logo"
         />
      </header>
   );
};

export default Icon;
